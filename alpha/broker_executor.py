"""
Broker Order Executor

Places live orders through the broker.

(Currently only a placeholder.)
"""


class BrokerExecutor:

    @staticmethod
    def execute(plan):

        raise NotImplementedError(
            "Broker execution not implemented yet."
        )